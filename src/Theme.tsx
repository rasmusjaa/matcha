import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
	palette: {
		primary: {
			main: '#49b960',
			contrastText: '#fff',
		},
		secondary: {
			main: '#222222',
			contrastText: '#fff',
		},
	},
})

export default theme
